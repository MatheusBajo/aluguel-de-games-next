# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
*.mp4
*.mov
*.avi
Thumbs.db

# credenciais de deploy (nunca commitar)
.env.deploy

# public/Organizado VOLTOU pro git em 12/ago/2026, depois da compressão.
# Motivo: sem a mídia no repositório o GitHub Actions não consegue buildar o
# site. Agora cabe (56 MB, maior arquivo 6,7 MB) porque os 467 MB de vídeo de
# manual/tutorial que o site NÃO usa saíram pra ../aluguel-midia-original/ e
# as fotos foram redimensionadas pra no máximo 1600px.
# ⚠️ O filtro LFS foi removido do .gitattributes: se voltar, vira ponteiro
# quebrado de novo. Leia o .gitattributes antes de mexer.
