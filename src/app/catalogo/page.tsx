import Catalogo from "@/components/catalogo/Catalogo";

export const metadata = {
    title: "Catálogo",
    description: "Veja todos os brinquedos disponíveis para locação",
};

export default function CatalogoPage() {
    return <Catalogo />;
}
