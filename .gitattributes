# LFS desativado em 2026-07-07: git-lfs não está instalado nesta máquina e o
# histórico antigo com LFS foi perdido na migração Windows→Mac. Mídia pesada
# (mp4/mov + public/Organizado) já fica fora do git via .gitignore.
